import { disableUserAction, resetAccessCodeAction } from "@/actions/accounts";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import type { UserRecord } from "@/domain/models";
import { formatDate, roleLabel } from "@/lib/utils";
export function UserTable({ users, returnTo }: { users: UserRecord[]; returnTo: string }) { return <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white card-shadow"><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-right text-sm"><thead className="bg-slate-50 text-xs text-slate-500"><tr><th className="p-4">الاسم</th><th className="p-4">الدور</th><th className="p-4">الحالة</th><th className="p-4">تاريخ الإنشاء</th><th className="p-4">إجراءات</th></tr></thead><tbody className="divide-y divide-slate-100">{users.map((user) => <tr key={user.id}><td className="p-4 font-bold">{user.displayName}</td><td className="p-4">{roleLabel(user.role)}</td><td className="p-4"><Badge tone={user.status === "active" ? "success" : "danger"}>{user.status === "active" ? "نشط" : "معطل"}</Badge></td><td className="p-4 text-slate-500">{formatDate(user.createdAt)}</td><td className="p-4"><div className="flex gap-2"><form action={resetAccessCodeAction}><input type="hidden" name="userId" value={user.id}/><input type="hidden" name="returnTo" value={returnTo}/><Button variant="secondary" size="sm">إعادة الرمز</Button></form>{user.status === "active" && <form action={disableUserAction}><input type="hidden" name="userId" value={user.id}/><input type="hidden" name="returnTo" value={returnTo}/><Button variant="danger" size="sm">تعطيل</Button></form>}</div></td></tr>)}</tbody></table></div></div>; }
