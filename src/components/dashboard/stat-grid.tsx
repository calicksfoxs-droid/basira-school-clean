import Link from "next/link";
export function StatGrid({ items }: { items: Array<{ label: string; value: number; href?: string }> }) { return <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{items.map((item) => { const content = <><strong className="text-3xl font-black text-[#0b1d33]">{item.value}</strong><span className="text-sm font-bold text-slate-500">{item.label}</span></>; return item.href ? <Link key={item.label} href={item.href} className="focus-ring flex min-h-28 flex-col justify-between rounded-2xl border border-slate-200 bg-white p-5 transition hover:-translate-y-0.5 hover:border-sky-200 card-shadow">{content}</Link> : <div key={item.label} className="flex min-h-28 flex-col justify-between rounded-2xl border border-slate-200 bg-white p-5 card-shadow">{content}</div>; })}</div>; }
